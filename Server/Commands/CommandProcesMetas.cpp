/* 
 * File:   CommandProcesMetas.cpp
 * Author: karsten
 * 
 * Created on 17. juli 2014, 17:22
 */

#include "CommandProcesMetas.h"
#include "Processor.h"
#include "../World/SWorld.h"
#include "../Sspacebjects/SMetaObj.h"
#include "../World/SWorld.h"
/*
CommandProcesMetas::CommandProcesMetas() : 
Command(world->getTime()){
}

uint32_t CommandProcesMetas::execute(){
	
	for (map<uint32_t, SMetaObj*>::iterator it = _processor->getLocalMetas().begin(); it != _processor->getLocalMetas().end(); it++){
		it->second->checkCollisions(_processor);
	}
	_time = world->getTime() + 40;
	return COMMAND_REPEAT;
}

CommandProcesMetas::~CommandProcesMetas() {
}
*/
